name: Deploy to GitHub Pages (main branch)

on:
  push:
    branches:
      - main  # This triggers the action only on pushes to the main branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
